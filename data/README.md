# Demo Data

```text
data/import-samples/
├── students_sample.xlsx
├── teachers_sample.xlsx
├── schedule_sample.xlsx
├── schedule_gv001_live_now.xlsx
└── qa/                    # Các biến thể dùng riêng cho QA/import lỗi
```

Ba file `*_sample.xlsx` là bộ dữ liệu cơ bản để import lần đầu. Các file trong `qa/` chỉ dùng để kiểm tra các tình huống khác nhau, không tự động nạp vào database.
